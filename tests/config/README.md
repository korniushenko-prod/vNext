# Config Tests

Host-side tests for the typed and versioned configuration model.

Stage 1 scope:
- validate factory defaults
- validate structured error aggregation
- validate key references and safety constraints

These tests must remain portable and must not depend on ESP-IDF.
