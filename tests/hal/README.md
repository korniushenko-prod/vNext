# HAL Tests

Host-side tests for Stage 3 HAL mocks.

Coverage includes:
- relay safe states
- digital input debounce with fake time
- analog scaling and clamp behavior
- pulse count and mock frequency injection
- PWM limits and safe state
- stepper skeleton behavior
- display buffer skeleton behavior
