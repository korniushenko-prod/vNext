// Template-specific UI logic

const TEMPLATE_API = window.SHIP_API?.endpoints || {};

function selectedBoardTemplateChipId(){
  const boardTemplateId=$('activeBoardTemplate')?.value||$('boardTemplateSelect')?.value||'';
  if(!boardTemplateId)return'';
  return state.templates?.board_templates?.[boardTemplateId]?.chip_template||'';
}

function renderTemplates(){if(!state.templates||!state.runtime)return;const chipTemplates=state.templates.chip_templates||{};const boardTemplates=state.templates.board_templates||{};const chipIds=Object.keys(chipTemplates);const boardIds=Object.keys(boardTemplates);$('activeChipTemplate').innerHTML=['<option value="">Follow board template</option>'].concat(chipIds.map(id=>'<option value="'+id+'">'+id+' - '+(chipTemplates[id].label||id)+'</option>')).join('');$('activeBoardTemplate').innerHTML=boardIds.map(id=>'<option value="'+id+'">'+id+' - '+(boardTemplates[id].label||id)+'</option>').join('');$('chipTemplateSelect').innerHTML=chipIds.map(id=>'<option value="'+id+'">'+id+' - '+(chipTemplates[id].label||id)+'</option>').join('');$('boardTemplateSelect').innerHTML=boardIds.map(id=>'<option value="'+id+'">'+id+' - '+(boardTemplates[id].label||id)+'</option>').join('');$('boardTemplateChipTemplate').innerHTML=chipIds.map(id=>'<option value="'+id+'">'+id+' - '+(chipTemplates[id].label||id)+'</option>').join('');$('templateBoardInstance').value=state.runtime.active_board||'';const runtimeBoardTemplate=state.runtime.board_template||state.templates.active_board_template||'';const runtimeChipTemplate=state.runtime.chip_template||state.templates.active_chip_template||'';if(runtimeBoardTemplate)$('activeBoardTemplate').value=runtimeBoardTemplate;$('activeChipTemplate').value=state.templates.active_chip_template||'';$('derivedChipTemplate').value=selectedBoardTemplateChipId()||'-';if(runtimeChipTemplate&&chipIds.includes(runtimeChipTemplate)){$('chipTemplateSelect').value=runtimeChipTemplate}else if(chipIds.length){$('chipTemplateSelect').value=chipIds[0]}if(runtimeBoardTemplate&&boardIds.includes(runtimeBoardTemplate)){$('boardTemplateSelect').value=runtimeBoardTemplate}else if(boardIds.length){$('boardTemplateSelect').value=boardIds[0]}loadSelectedTemplateIntoEditor('chip');loadSelectedTemplateIntoEditor('board');renderBoardTemplatePhysicalMap()}
function loadSelectedTemplateIntoEditor(type){const collection=type==='chip'?(state.templates?.chip_templates||{}):(state.templates?.board_templates||{});const selectId=type==='chip'?'chipTemplateSelect':'boardTemplateSelect';const idField=type==='chip'?'chipTemplateId':'boardTemplateId';const templateId=$(selectId).value;const template=collection[templateId]||{};$(idField).value=templateId||'';if(type==='chip'){state.ui.lastAddedChipPin='';state.ui.chipPinOrder=Object.keys(template.pins||{});writeChipTemplateToEditors(template);renderChipPinsVisual()}else{state.ui.lastAddedBoardRule=-1;writeBoardTemplateToEditors(template);renderBoardRulesVisual()}}
function setTemplateTab(tab){document.querySelectorAll('.template-tabs button').forEach(btn=>btn.classList.toggle('active',btn.dataset.templateTab===tab));$('template-panel-chip').classList.toggle('hidden',tab!=='chip');$('template-panel-board').classList.toggle('hidden',tab!=='board')}
function renderBoardTemplatePhysicalMap(){const target=$('boardPhysicalTable');if(!target)return;const rows=(state.hardware?.pins||[]).filter(pin=>pin.class!=='exclusive').map(pin=>'<tr><td>GPIO'+pin.gpio+'</td><td><span class="badge '+pin.class+'">'+pin.class+'</span></td><td>'+(pin.available?'yes':'no')+'</td><td>'+(pin.owner||'-')+'</td><td>'+(pin.reason||'-')+'</td></tr>').join('');target.innerHTML=rows||'<tr><td colspan="5">No hardware map available</td></tr>'}
async function saveTemplateSelection(){$('templateSelectionStatus').textContent='Assigning templates to active board...';const payload={active_chip_template:$('activeChipTemplate').value,active_board_template:$('activeBoardTemplate').value,active_board:state.runtime?.active_board||$('activeBoard').value};try{const r=await getJson(TEMPLATE_API.templateSelection||'/template-selection',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const refreshOk=await refreshTemplateSurface();$('templateSelectionStatus').textContent=refreshOk?(r.message||'Templates assigned'):'Templates assigned with refresh warnings'}catch(e){$('templateSelectionStatus').textContent='Save failed: '+e.message}}
async function saveTemplate(type,applyAfterSave=false){const statusId=type==='chip'?'chipTemplateStatus':'boardTemplateStatus';const idField=type==='chip'?'chipTemplateId':'boardTemplateId';$(statusId).textContent='Saving template...';try{if(type==='chip')syncChipVisualToJson();else syncBoardVisualToJson();const template=JSON.parse((type==='chip'?$('chipTemplateEditor').value:$('boardTemplateEditor').value)||'{}');const payload={type:type,template_id:$(idField).value.trim(),template:template};if(!payload.template_id)throw new Error('Template ID is required');const r=await getJson(TEMPLATE_API.templateLibrary||'/template-library',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const refreshOk=await refreshTemplateSurface();if(type==='chip'){$('chipTemplateSelect').value=payload.template_id}else{$('boardTemplateSelect').value=payload.template_id}loadSelectedTemplateIntoEditor(type);if(applyAfterSave){if(type==='chip'){$('activeChipTemplate').value=payload.template_id}else{$('activeBoardTemplate').value=payload.template_id;$('derivedChipTemplate').value=selectedBoardTemplateChipId()||'-';$('activeChipTemplate').value=''}await saveTemplateSelection();$(statusId).textContent='Template saved and applied'}else{$(statusId).textContent=refreshOk?(r.message||'Template saved'):'Template saved with refresh warnings'}}catch(e){$(statusId).textContent='Save failed: '+e.message}}
async function deleteTemplate(type){const selectId=type==='chip'?'chipTemplateSelect':'boardTemplateSelect';const statusId=type==='chip'?'chipTemplateStatus':'boardTemplateStatus';const templateId=$(selectId).value;if(!templateId)return;if(!confirm('Delete '+type+' template '+templateId+'?'))return;$(statusId).textContent='Deleting template...';try{const r=await getJson(TEMPLATE_API.templateDelete||'/template-delete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:type,template_id:templateId})});const refreshOk=await refreshTemplateSurface();$(statusId).textContent=refreshOk?(r.message||'Template deleted'):'Template deleted with refresh warnings'}catch(e){$(statusId).textContent='Delete failed: '+e.message}}
async function seedChipTemplate(){$('chipTemplateStatus').textContent='Loading built-in seed...';try{const template=await getJson(TEMPLATE_API.builtInTemplate||'/template');writeChipTemplateToEditors({label:template.name||template.id,pins:Object.fromEntries((template.pins||[]).map(pin=>[String(pin.gpio),{capabilities:pin.capabilities||[],internal_pullup:!!pin.internal_pullup,input_only:!!pin.input_only,strapping:!!pin.strapping,forbidden:!!pin.forbidden,note:pin.note||''}]))});renderChipPinsVisual();if(!$('chipTemplateId').value){$('chipTemplateId').value=(template.id||'chip_template')+'_custom'}$('chipTemplateStatus').textContent='Built-in seed loaded'}catch(e){$('chipTemplateStatus').textContent='Seed failed: '+e.message}}
function newChipTemplate(){const base=getEditableChipTemplate();state.ui.lastAddedChipPin='';state.ui.chipPinOrder=Object.keys(base.pins||{});writeChipTemplateToEditors({label:(base.label||'Current Chip Template')+' Copy',pins:base.pins||{}});renderChipPinsVisual();$('chipTemplateId').value='chip_template_custom';$('chipTemplateStatus').textContent='Started a new chip template from the current one'}
function newBoardTemplate(){const base=getEditableBoardTemplate();const activeChip=$('derivedChipTemplate').value&&$('derivedChipTemplate').value!=='-'?$('derivedChipTemplate').value:($('activeChipTemplate').value||'');writeBoardTemplateToEditors({label:(base.label||'Current Board Template')+' Copy',chip_template:base.chip_template||activeChip,rules:Array.isArray(base.rules)?base.rules:[]});renderBoardRulesVisual();$('boardTemplateId').value='board_template_custom';$('boardTemplateStatus').textContent='Started a new board template from the current one'}
function addChipPin(){const template=getEditableChipTemplate();let gpio=4;while(template.pins&&template.pins[String(gpio)])gpio++;template.pins=template.pins||{};template.pins[String(gpio)]={capabilities:['di'],internal_pullup:true,input_only:false,strapping:false,forbidden:false,note:''};state.ui.lastAddedChipPin=String(gpio);state.ui.chipPinOrder=[...state.ui.chipPinOrder.filter(v=>v!==String(gpio)),String(gpio)];writeChipTemplateToEditors(template);renderChipPinsVisual();$('chipTemplateStatus').textContent='Added GPIO'+gpio+' at the end of the chip template'}
function removeChipPin(gpio){const template=getEditableChipTemplate();if(template.pins)delete template.pins[gpio];state.ui.chipPinOrder=state.ui.chipPinOrder.filter(v=>v!==gpio);writeChipTemplateToEditors(template);renderChipPinsVisual();$('chipTemplateStatus').textContent='Pin removed'}
function addBoardRule(){const template=getEditableBoardTemplate();template.rules=Array.isArray(template.rules)?template.rules:[];template.rules.push({id:'rule_'+(template.rules.length+1),feature:'',class:'warning',owner:'',reason:'',always_on:false,pins:[]});state.ui.lastAddedBoardRule=template.rules.length-1;writeBoardTemplateToEditors(template);renderBoardRulesVisual();$('boardTemplateStatus').textContent='Rule added at the end of the board template'}
function removeBoardRule(index){const template=getEditableBoardTemplate();template.rules=Array.isArray(template.rules)?template.rules:[];template.rules.splice(index,1);writeBoardTemplateToEditors(template);renderBoardRulesVisual();$('boardTemplateStatus').textContent='Rule removed'}
