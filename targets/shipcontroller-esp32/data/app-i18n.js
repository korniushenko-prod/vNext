window.SHIP_I18N = {ru:{languageLabel:'Язык',modeLabel:'Режим',signalsSummary:'? Справка по сигналам',selectorSummary:'? Справка по selector',signalsBody:'<p><strong>Сигнал</strong> это осмысленное значение системы, а не просто пин.</p><p><strong>Resource-backed</strong> сигналы зеркалят реальные каналы: DI, DO, AI.</p><p><strong>Derived</strong> сигналы вычисляются из других сигналов и нужны для логики, UI, alarm и sequence.</p><p><strong>Units</strong> нужны для инженерного смысла: число <code>42</code> без единиц непонятно, а <code>42 C</code> или <code>42 %</code> уже полезно для оператора и сервиса.</p><p><strong>Raw</strong> показывает сырое значение источника, <strong>Value</strong> показывает инженерное значение после обработки.</p>',selectorBody:'<p><strong>Selector</strong> выбирает один из двух входных сигналов и публикует выходной сигнал.</p><p><strong>Primary</strong> это основной источник, <strong>Secondary</strong> запасной или подменный.</p><p><strong>Select</strong> это бинарный сигнал выбора: <code>false</code> -> primary, <code>true</code> -> secondary.</p><p>Типичный сценарий: локальный датчик + удалённое значение по шине + сервисная подмена.</p>',signalsOverview:'Сигналы это сервисный runtime-слой контроллера. Физические сигналы зеркалят каналы, а вычисляемые помогают строить маршрутизацию, подмену и дальнейшую логику.',blocksOverview:'Блоки это логические элементы контроллера. Создавай их только по необходимости: timer для задержек и selector для выбора источника сигнала.',editorNote:'Пока редактор поддерживает <code>substitute</code> сигналы: если сигнал enable равен true, вместо primary публикуется substitute. Selector block живёт в blocks и будет настраиваться отдельным редактором.'},en:{languageLabel:'Language',modeLabel:'Mode',signalsSummary:'? Signals help',selectorSummary:'? Selector block help',signalsBody:'<p><strong>Signal</strong> is a meaningful runtime value, not just a pin.</p><p><strong>Resource-backed</strong> signals mirror real DI, DO and AI channels.</p><p><strong>Derived</strong> signals are computed from other signals and are used by logic, UI, alarms and sequences.</p><p><strong>Units</strong> give engineering meaning: plain <code>42</code> is ambiguous, while <code>42 C</code> or <code>42 %</code> is useful for operators and service.</p><p><strong>Raw</strong> shows the source value, while <strong>Value</strong> shows the engineering value after processing.</p>',selectorBody:'<p><strong>Selector</strong> chooses one of two input signals and publishes one output signal.</p><p><strong>Primary</strong> is the default source, <strong>Secondary</strong> is the backup or override source.</p><p><strong>Select</strong> is a binary choice signal: <code>false</code> -> primary, <code>true</code> -> secondary.</p><p>Typical use case: local sensor + bus value + service override.</p>',signalsOverview:'Signals are the service-level runtime view of the controller. Resource-backed signals mirror physical channels. Derived signals can implement routing and substitution before later blocks or sequences consume them.',blocksOverview:'Blocks are the logic elements of the controller. Create them only when needed: timers for delays and selectors for source switching.',editorNote:'The editor currently supports <code>substitute</code> signals: when enable is true, the substitute source is published instead of the primary source. Selector blocks live in the blocks layer and will get a dedicated editor.'}};
