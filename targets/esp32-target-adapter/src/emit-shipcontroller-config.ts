import type { RuntimePack } from "@universal-plc/runtime-pack-schema";

export function emitShipControllerConfigArtifact(_pack: RuntimePack): never {
  throw new Error("ShipController config artifact emission is not implemented in PR-12A.");
}