# Repository Structure Note

Canonical planning documents live in `docs/delivery`.
Canonical architecture documents live in `docs/architecture`.
Merge/foundation execution notes live in `docs/merge`.

`docs/planning` was intentionally removed to avoid duplicate planning sources and future divergence.
