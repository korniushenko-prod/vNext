# ADR-0002 System Signals vs Composition Routes

Accepted. `system.signals` are canonical in System scope. `composition.routes` are canonical in Composition scope. Normalization happens in semantic build/materializer.
